fastapi==0.115.12
python-multipart==0.0.20
pydantic==2.11.3
uvicorn==0.34.2
mangum==0.19.0

# Analysis pipeline — editable install from monorepo root.
# For Vercel deployment this will need to be packaged or installed via a build step.
# Local dev: pip install -e /path/to/BODAQS/analysis
# bodaqs-analysis is not on PyPI; see api/README or SPEC.md for deployment notes.
numpy
pandas
scipy
PyYAML
fitparse
