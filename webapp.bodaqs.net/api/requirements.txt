fastapi==0.115.12
python-multipart==0.0.20
pydantic==2.11.3
uvicorn==0.34.2
mangum==0.19.0

# Analysis pipeline — installed from monorepo sibling directory.
# pip resolves file: paths relative to the requirements.txt file location (api/).
# ../../analysis from api/ == BODAQS/analysis/ (monorepo root).
bodaqs-analysis @ file:../../analysis
numpy
pandas
scipy
PyYAML
fitparse
