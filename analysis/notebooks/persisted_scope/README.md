# Persisted Scope Notebooks

Place remediated notebooks here when they consume a saved Study Set or other
persisted analysis scope.

These notebooks should not contain their own session selector. The scope should
come from the Python Library Adapter so the notebook, Library API service, and
web application share the same Study Set contract.
