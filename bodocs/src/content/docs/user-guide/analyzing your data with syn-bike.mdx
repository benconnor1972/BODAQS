---
title: Analysis with syn.bike

sidebar:
  order: 3
---
import { Aside, Card, CardGrid, Steps } from '@astrojs/starlight/components';
import Lede from '@components/Lede.astro';
import Flag from '@components/Flag.astro';
import Lightbox from '@components/Lightbox.astro';

<Lede>
**syn.bike** have a very nice [website](https://data.syn.bike) that can be used to visualize BODAQS data. If you don't have an appetite for Jupyter Lab notebooks, this is a great way to view your logs.
</Lede>


<Aside type="note" title="BODAQS Import Manager recommended">
<Flag src="/src/assets/pics/user-guide/use-import-manager.png" alt="use the import manager">

We recommend you process your files with the BODAQS Import Manager, with the 'syn.bike export' option checked, prior to upload to data.syn.bike. 
</Flag>
</Aside>

## Start synDAQ_server.exe
Navigate to the local executable you downloaded from data.syn.bike. Double-click the executable to run it. A terminal window should openAs you use data.syn.bike this terminal window will report what it is doing (if you're interested).

## Checking the configuration of  data.syn.bike
Navigate to [data.syn.bike](http://data.syn.bike) in your web browser. Check the `device' and `settings` tabs and ensure the settings are correct. See [setting up data.syn.bike](/software-guide/setup-syn-bike/).

<Aside type="note" title="Helper files">
If you processed your files with the BODAQS Import Manager, each run has a small 'helper' text file to show you the correct settings.
</Aside>


## Upload and examine your files
<Flag src="/src/assets/pics/user-guide/synoutputs.png" alt="synoutputs">
Go to the 'analysis' tab.
If you are measuring shock travel rather than rear wheel travel (as you almost certainly are), then you will need to supply leverage information. 

If you have a leverage curve for your bike, then select 'curve data' and upload the curve in a CSV file. An example of the format is [here]. Otherwise select 'single value', divide your bike's rear wheel travel by its shock travel and enter the result in the box labelled 'average leverage'.

Click in the 'file' box. Select the file you want to view. 

The 'basic' and 'histogram' plots let you examine time-series and frequency distribution data on wheel travel and velocity. 
</Flag>

